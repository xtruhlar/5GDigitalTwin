apiVersion: 2

providers:
  - name: srsRAN # A uniquely identifiable name for the provider
    folder: srsran # The folder where to place the dashboards
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/srs/
